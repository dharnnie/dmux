{
  "name": "dmux-core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared parser and orchestration primitives for dmux. Consumed by dmux.sh and the dmux-ui server.",
  "type": "module",
  "main": "src/index.js",
  "exports": {
    ".": "./src/index.js",
    "./config": "./src/config.js"
  },
  "bin": {
    "dmux-parse-config": "./bin/parse-config.js"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
